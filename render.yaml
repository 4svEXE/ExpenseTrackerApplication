services:
  - type: static
    name: expense-tracker
    runtime: static
    buildCommand: npm install && npm run build
    publishPath: dist/expense-tracker-application/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
